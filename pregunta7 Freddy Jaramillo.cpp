#include <iostream>
using namespace std;
main(){
    int x, numerador=2, denominador=1, nvalores, operacion;

    cout << "Ingrese el numero de valores de la serie: "<<endl;
    cin >> nvalores;
    operacion == ((numerador/denominador)-((numerador+denominador)/(denominador+2))+(++numerador/++denominador)-((numerador+denominador)/(denominador+2)));
    x == operacion;
    cout << " El valor de x sera "<< x <<endl;

}